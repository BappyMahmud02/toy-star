import React from 'react';
import { useLoaderData } from 'react-router-dom';




const Update = () => {
    // const update = useLoaderData()
    // console.log(update);

    // const { _id, name, price, sellerName, rating, quantity, sellerEmail, category, photo } = update
    return (
        <div>

        </div>
    );
};

export default Update;